name: 'Create Pre-release with Release-Please'

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        required: true
        default: 'stag'
        description: 'Environment for pre-release'
        options:
          - dev
          - stag
          - beta
      branch:
        type: string
        required: true
        default: 'main'
        description: 'Branch to create pre-release from'

permissions:
  contents: write
  pull-requests: write

jobs:
  create-prerelease:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: '0'
          token: ${{ secrets.PRE_RELEASE_TAG_ACTION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: 'package.json'

      - name: Install dependencies
        run: npm ci

      - name: Create Pre-release with Release-Please CLI
        env:
          GITHUB_TOKEN: ${{ secrets.PRE_RELEASE_TAG_ACTION }}
        run: |
          echo "🚀 Creating pre-release for ${{ github.event.inputs.environment }} environment"
          echo "Branch: ${{ github.event.inputs.branch }}"
          
          # Create temporary release-please config for this environment
          cat > release-please-prerelease.json << EOF
          {
            "\$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
            "bootstrap-sha": "75e32df06af5f8023b7b38b38354751d7223fce4",
            "include-component-in-tag": false,
            "changelog-path": "CHANGELOG.md",
            "branches": [
              { 
                "branch": "${{ github.event.inputs.branch }}", 
                "prerelease": true,
                "prerelease-label": "${{ github.event.inputs.environment }}"
              }
            ],
            "packages": {
              ".": {
                "release-type": "node",
                "package-name": "react-webpack-demo",
                "prerelease": true,
                "prerelease-type": "${{ github.event.inputs.environment }}",
                "extra-files": [
                  "package.json"
                ]
              }
            }
          }
          EOF
          
          echo "✅ Created temporary release-please config"
          
          # Step 1: Create/update release PR to calculate version
          echo "🔄 Step 1: Creating release PR..."
          npx release-please release-pr \
            --config-file=release-please-prerelease.json \
            --repo-url=https://github.com/deepaksmallcase/test-release-please \
            --token=$GITHUB_TOKEN \
            --target-branch=${{ github.event.inputs.branch }}
          
          echo "✅ Release PR created/updated"
          
          # Step 2: Create GitHub release (with error handling for duplicates)
          echo "🔄 Step 2: Creating GitHub release..."
          if npx release-please github-release \
            --config-file=release-please-prerelease.json \
            --repo-url=https://github.com/deepaksmallcase/test-release-please \
            --token=$GITHUB_TOKEN \
            --target-branch=${{ github.event.inputs.branch }}; then
            echo "✅ Pre-release created successfully!"
          else
            echo "⚠️ Release may already exist or no changes to release"
            echo "✅ Checking existing tags..."
            git tag --sort=-version:refname | head -5
          fi
          
          echo "📦 Tag format: v1.4.0-${{ github.event.inputs.environment }}.0 → v1.4.0-${{ github.event.inputs.environment }}.1"
          echo "🌍 Environment: ${{ github.event.inputs.environment }}"
          echo "🌿 Branch: ${{ github.event.inputs.branch }}"

      - name: Cleanup
        if: always()
        run: |
          rm -f release-please-prerelease.json
          echo "🧹 Cleaned up temporary files"