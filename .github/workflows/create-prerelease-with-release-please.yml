name: 'Create Pre-release with Release-Please'

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        required: true
        default: 'stag'
        description: 'Environment for pre-release'
        options:
          - dev
          - stag
          - beta
      branch:
        type: string
        required: true
        default: 'main'
        description: 'Branch to create pre-release from'

permissions:
  contents: write
  pull-requests: write

jobs:
  create-prerelease:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: '0'
          token: ${{ secrets.PRE_RELEASE_TAG_ACTION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: 'package.json'

      - name: Install dependencies
        run: npm ci

      - name: Create Pre-release with Release-Please (No PRs)
        env:
          GITHUB_TOKEN: ${{ secrets.PRE_RELEASE_TAG_ACTION }}
        run: |
          echo "🚀 Creating pre-release for ${{ github.event.inputs.environment }} environment"
          echo "Branch: ${{ github.event.inputs.branch }}"
          echo "❌ No PRs will be created - using direct release approach"
          
          # Get current version for reference
          currentVersion=$(cat package.json | jq -r '.version')
          echo "📋 Current version: $currentVersion"
          
          # Create temporary release-please config for this environment
          cat > release-please-prerelease.json << EOF
          {
            "\$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
            "bootstrap-sha": "75e32df06af5f8023b7b38b38354751d7223fce4",
            "include-component-in-tag": false,
            "changelog-path": "CHANGELOG.md",
            "branches": [
              { 
                "branch": "${{ github.event.inputs.branch }}", 
                "prerelease": true,
                "prerelease-label": "${{ github.event.inputs.environment }}"
              }
            ],
            "packages": {
              ".": {
                "release-type": "node",
                "package-name": "react-webpack-demo",
                "prerelease": true,
                "prerelease-type": "${{ github.event.inputs.environment }}",
                "extra-files": [
                  "package.json"
                ]
              }
            }
          }
          EOF
          
          # Create manifest file to track versions
          cat > .release-please-manifest.json << EOF
          {
            ".": "$currentVersion"
          }
          EOF
          
          echo "✅ Created temporary release-please config and manifest"
          
          # Use only github-release command (no PR creation)
          echo "🔄 Creating GitHub release directly (no PRs)..."
          if npx release-please github-release \
            --config-file=release-please-prerelease.json \
            --manifest-file=.release-please-manifest.json \
            --repo-url=https://github.com/deepaksmallcase/test-release-please \
            --token=$GITHUB_TOKEN \
            --target-branch=${{ github.event.inputs.branch }}; then
            
            echo "✅ Pre-release created successfully!"
            
            # Get the latest tag that was just created
            echo "🔍 Finding the newly created tag..."
            latestTag=$(git tag --sort=-version:refname | grep -E "v.*-${{ github.event.inputs.environment }}\." | head -1)
            
            if [ -n "$latestTag" ]; then
              echo "🏷️ Found new tag: $latestTag"
              
              # Push the tag to trigger deployment
              echo "📤 Pushing tag to trigger ${{ github.event.inputs.environment }} deployment..."
              git push origin $latestTag
              
              echo "✅ Tag pushed successfully!"
              echo "🚀 This will trigger the ${{ github.event.inputs.environment }}-deployment.yml workflow"
              echo "📦 Version progression: $currentVersion → $latestTag"
            else
              echo "⚠️ No new tag found - release may not have been created"
            fi
            
          else
            echo "⚠️ No release created - this usually means no releasable changes found"
            echo "💡 Release-please looks for conventional commits (feat:, fix:, etc.)"
            echo "✅ Checking existing tags..."
            git tag --sort=-version:refname | head -5
          fi
          
          echo "🌍 Environment: ${{ github.event.inputs.environment }}"
          echo "🌿 Branch: ${{ github.event.inputs.branch }}"
          echo "💡 Release-please calculates versions automatically based on conventional commits"

      - name: Cleanup
        if: always()
        run: |
          rm -f release-please-prerelease.json
          rm -f .release-please-manifest.json
          echo "🧹 Cleaned up temporary files"